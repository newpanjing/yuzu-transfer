import { CheckCircle2, CircleHelp, Link2, Settings, Upload } from 'lucide-react';
type Props = { active: 'connect' | 'transfer'; onSelect: (value: 'connect' | 'transfer') => void };
export function Sidebar({ active, onSelect }: Props) { const items = [{ id: 'connect' as const, label: '连接设备', icon: Link2 }, { id: 'transfer' as const, label: '传输中', icon: Upload }, { id: 'done' as const, label: '已完成', icon: CheckCircle2 }]; return <aside className="sidebar">{items.map(({ id, label, icon: Icon }) => <button key={id} className={active === id ? 'nav-item active' : 'nav-item'} onClick={() => id !== 'done' && onSelect(id)}><Icon size={18} />{label}{id === 'connect' && <i />}</button>)}<div className="nav-spacer" /><button className="nav-item"><Settings size={18} />设置</button><button className="nav-item"><CircleHelp size={18} />关于</button></aside>; }
